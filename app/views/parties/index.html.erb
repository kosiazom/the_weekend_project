<h1 class= "title is-2">List of Parties</h1>
<ul>
    <% @parties.each do|party| %>
   <li> <%= link_to party.name, party_path(party) %></li>
        <%= "Location - #{party.location}"%>

   <%end%>
</ul>
        <%= link_to "My Parties", party_goer_path(current_user)%>
